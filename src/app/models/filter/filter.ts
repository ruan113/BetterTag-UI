export class Filter {
    onlyShow: {
        showImages: boolean;
        showGifs: boolean;
        showVideos: boolean;
    };
    modes: {
        fullScreen: boolean
    };

    constructor() {
    }
}